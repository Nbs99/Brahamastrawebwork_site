/* ============================================
   PARALLAX DEPTH SCROLL EFFECT
   Apple TV+ Style 3D Depth on Scroll
   ============================================ */

.parallax-wrap {
    --parallax-speed: 0.15;
    perspective: 1200px;
    perspective-origin: center center;
    transform-style: preserve-3d;
}

.parallax-card {
    transform-style: preserve-3d;
    will-change: transform;
    transition: transform 0.1s linear;
}

/* Image Layer - Slowest (Background) */
.p-depth-img {
    transform: translateZ(-40px) scale(1.12);
    will-change: transform;
    transition: transform 0.1s linear;
    filter: brightness(0.9);
}

/* Content Layer - Medium */
.p-depth-content {
    transform: translateZ(-15px);
    will-change: transform;
    transition: transform 0.1s linear;
}

/* Badge Layer - Fastest (Closest to viewer) */
.p-depth-badge {
    transform: translateZ(25px);
    will-change: transform;
    transition: transform 0.1s linear;
}

/* Logo 3D Parallax on Scroll */
.logo-3d-container {
    transform-style: preserve-3d;
    perspective: 800px;
    will-change: transform;
}

.logo-3d-inner {
    transform-style: preserve-3d;
    transition: transform 0.15s ease-out;
    will-change: transform;
}

/* Nav Logo Special Parallax */
#nav-logo-container .logo-3d-inner {
    transform: translateZ(0px);
}

/* Hero Parallax Card Enhanced */
.parallax-card:hover .p-depth-img {
    transform: translateZ(-50px) scale(1.18);
    filter: brightness(1);
}

.parallax-card:hover .p-depth-content {
    transform: translateZ(-20px);
}

.parallax-card:hover .p-depth-badge {
    transform: translateZ(35px);
}

/* Floating Elements Parallax */
.float-parallax {
    will-change: transform;
    transition: transform 0.2s ease-out;
}

/* Section Headers Parallax */
.parallax-header {
    transform: translateZ(20px);
    will-change: transform;
    transition: transform 0.15s ease-out;
}

/* Deal Cards Parallax */
.deal-price {
    transform: translateZ(15px);
    will-change: transform;
    transition: transform 0.15s ease-out;
}

/* Grid Items Subtle Parallax */
#games-grid > div {
    transform-style: preserve-3d;
    transition: transform 0.3s ease-out, box-shadow 0.3s ease-out;
    will-change: transform;
}

#games-grid > div:hover {
    transform: translateZ(20px) scale(1.03);
    box-shadow: 0 20px 60px rgba(6, 182, 212, 0.2);
}

/* Elite Card Parallax */
.elite-content {
    transform: translateZ(10px);
    will-change: transform;
    transition: transform 0.15s ease-out;
}

.elite-badge {
    transform: translateZ(30px);
    will-change: transform;
    transition: transform 0.15s ease-out;
}

/* Flip Cards 3D Setup */
.flip-card-3d-box {
    perspective: 1200px;
    height: 320px;
}

.flip-card-3d-inner {
    position: relative;
    width: 100%;
    height: 100%;
    transition: transform 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    transform-style: preserve-3d;
    will-change: transform;
}

.flip-card-3d-box:hover .flip-card-3d-inner {
    transform: rotateY(180deg);
}

.flip-card-3d-front,
.flip-card-3d-back {
    position: absolute;
    width: 100%;
    height: 100%;
    -webkit-backface-visibility: hidden;
    backface-visibility: hidden;
    border-radius: 2rem;
    overflow: hidden;
}

.flip-card-3d-front {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
}

.flip-card-3d-back {
    transform: rotateY(180deg);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
}

/* Text 3D Pop Effect */
.text-3d-pop {
    text-shadow: 
        0 1px 0 #ccc,
        0 2px 0 #c9c9c9,
        0 3px 0 #bbb,
        0 4px 0 #b9b9b9,
        0 5px 0 #aaa,
        0 6px 1px rgba(0,0,0,.1),
        0 0 5px rgba(0,0,0,.1),
        0 1px 3px rgba(0,0,0,.3),
        0 3px 5px rgba(0,0,0,.2),
        0 5px 10px rgba(0,0,0,.25),
        0 10px 10px rgba(0,0,0,.2),
        0 20px 20px rgba(0,0,0,.15);
}

/* Glitch Hover Effect */
.glitch-hover:hover {
    animation: glitch 0.3s ease forwards;
}

@keyframes glitch {
    0% { transform: translate(0); }
    20% { transform: translate(-2px, 2px); }
    40% { transform: translate(-2px, -2px); }
    60% { transform: translate(2px, 2px); }
    80% { transform: translate(2px, -2px); }
    100% { transform: translate(0); }
}

/* Halo Rings */
.halo-ring {
    position: absolute;
    border: 1px solid rgba(6, 182, 212, 0.15);
    border-radius: 50%;
    animation: halo-pulse 4s ease-in-out infinite;
    pointer-events: none;
}

.halo-ring-2 {
    position: absolute;
    border: 1px solid rgba(124, 58, 237, 0.1);
    border-radius: 50%;
    animation: halo-pulse 6s ease-in-out infinite reverse;
    pointer-events: none;
}

@keyframes halo-pulse {
    0%, 100% { transform: scale(1); opacity: 0.3; }
    50% { transform: scale(1.15); opacity: 0.6; }
}

/* Cyber Grid 3D */
.cyber-grid-3d {
    position: absolute;
    inset: 0;
    background-image: 
        linear-gradient(rgba(6, 182, 212, 0.03) 1px, transparent 1px),
        linear-gradient(90deg, rgba(6, 182, 212, 0.03) 1px, transparent 1px);
    background-size: 60px 60px;
    transform: perspective(500px) rotateX(60deg);
    transform-origin: center top;
    pointer-events: none;
    mask-image: linear-gradient(to bottom, transparent, black 30%, black 70%, transparent);
    -webkit-mask-image: linear-gradient(to bottom, transparent, black 30%, black 70%, transparent);
}

/* Scene & Cube for Future Releases */
.scene {
    width: 200px;
    height: 200px;
    perspective: 600px;
    margin: 0 auto;
}

.cube {
    width: 100%;
    height: 100%;
    position: relative;
    transform-style: preserve-3d;
    animation: cube-rotate 20s linear infinite;
    will-change: transform;
}

@keyframes cube-rotate {
    0% { transform: rotateX(0deg) rotateY(0deg); }
    100% { transform: rotateX(360deg) rotateY(360deg); }
}

.cube-face {
    position: absolute;
    width: 200px;
    height: 200px;
    border: 1px solid rgba(6, 182, 212, 0.3);
    border-radius: 1rem;
    overflow: hidden;
    backface-visibility: visible;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(0,0,0,0.8);
}

.cube-face img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.6;
    border-radius: 1rem;
}

.cube-face:nth-child(1) { transform: rotateY(0deg) translateZ(100px); }
.cube-face:nth-child(2) { transform: rotateY(90deg) translateZ(100px); }
.cube-face:nth-child(3) { transform: rotateY(180deg) translateZ(100px); }
.cube-face:nth-child(4) { transform: rotateY(270deg) translateZ(100px); }
.cube-face:nth-child(5) { transform: rotateX(90deg) translateZ(100px); }
.cube-face:nth-child(6) { transform: rotateX(-90deg) translateZ(100px); }

/* Carousel 3D */
.carousel-3d {
    perspective: 1200px;
    transform-style: preserve-3d;
}

.carousel-card {
    position: absolute;
    width: 220px;
    height: 340px;
    left: 50%;
    top: 50%;
    border-radius: 1.5rem;
    overflow: hidden;
    transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
    cursor: pointer;
    transform-style: preserve-3d;
    box-shadow: 0 10px 40px rgba(0,0,0,0.5);
}

.carousel-card img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.carousel-card .card-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, transparent 60%);
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 1.5rem;
}

/* Cyber Terminal */
.cyber-term-container {
    perspective: 800px;
}

.cyber-terminal {
    background: linear-gradient(145deg, rgba(6, 182, 212, 0.05), rgba(0, 0, 0, 0.9));
    border: 1px solid rgba(6, 182, 212, 0.2);
    position: relative;
    overflow: hidden;
}

.terminal-screen {
    background: repeating-linear-gradient(
        0deg,
        rgba(6, 182, 212, 0.03) 0px,
        rgba(6, 182, 212, 0.03) 1px,
        transparent 1px,
        transparent 3px
    );
    pointer-events: none;
}

.terminal-float {
    animation: terminal-hover 6s ease-in-out infinite;
}

@keyframes terminal-hover {
    0%, 100% { transform: translateY(0px) rotateX(0deg); }
    50% { transform: translateY(-8px) rotateX(1deg); }
}

/* Elite Card */
.elite-container {
    perspective: 1000px;
}

.elite-card {
    background: linear-gradient(135deg, rgba(0,0,0,0.95), rgba(20,20,20,0.9));
    border: 1px solid rgba(255, 215, 0, 0.2);
    transform-style: preserve-3d;
    transition: transform 0.3s ease-out;
    will-change: transform;
}

.elite-glare {
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at var(--mouse-x, 50%) var(--mouse-y, 50%), rgba(255,255,255,0.06) 0%, transparent 60%);
    pointer-events: none;
    z-index: 5;
    transition: background 0.3s ease;
}

/* Confetti */
.confetti {
    position: absolute;
    bottom: -10%;
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #06b6d4;
    animation: confetti-rise 15s linear infinite;
    opacity: 0;
}

@keyframes confetti-rise {
    0% { transform: translateY(0) rotate(0deg); opacity: 0; }
    10% { opacity: 0.8; }
    90% { opacity: 0.8; }
    100% { transform: translateY(-110vh) rotate(720deg); opacity: 0; }
}

/* Fade In Animation */
.fade-in {
    animation: fadeIn 0.3s ease-out;
}

@keyframes fadeIn {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
}

/* No Scrollbar */
.no-scrollbar::-webkit-scrollbar { display: none; }
.no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

/* Performance Mode */
.performance-mode * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
}

.performance-mode .confetti,
.performance-mode .cyber-grid-3d,
.performance-mode .terminal-screen,
.performance-mode .halo-ring,
.performance-mode .halo-ring-2 {
    display: none !important;
}

/* 3D Transform Helpers */
.transform-style-3d {
    transform-style: preserve-3d;
}

.perspective-1000 {
    perspective: 1000px;
}

/* Smooth Scroll Behavior */
html {
    scroll-behavior: smooth;
}

/* Custom Scrollbar for modals */
.overflow-y-auto::-webkit-scrollbar {
    width: 4px;
}

.overflow-y-auto::-webkit-scrollbar-track {
    background: transparent;
}

.overflow-y-auto::-webkit-scrollbar-thumb {
    background: rgba(6, 182, 212, 0.3);
    border-radius: 10px;
}

.overflow-y-auto::-webkit-scrollbar-thumb:hover {
    background: rgba(6, 182, 212, 0.6);
}

/* Parallax Active State Indicator */
.parallax-active .parallax-card {
    transition: transform 0.05s linear;
}
/* Ticket System Styles */
#ticket-panel {
    animation: slideInRight 0.3s ease-out;
}

@keyframes slideInRight {
    from { transform: translateX(100%); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
}

#ticket-messages-container::-webkit-scrollbar,
#admin-chat-messages::-webkit-scrollbar {
    width: 4px;
}

#ticket-messages-container::-webkit-scrollbar-thumb,
#admin-chat-messages::-webkit-scrollbar-thumb {
    background: rgba(147, 51, 234, 0.3);
    border-radius: 2px;
}
